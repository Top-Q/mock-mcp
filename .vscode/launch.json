{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch MCP Server (dev)",
      "program": "${workspaceFolder}/src/server.ts",
      "preLaunchTask": "",
      "runtimeExecutable": "npx",
      "runtimeArgs": ["tsx", "src/server.ts"],
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "node",
      "request": "launch",
      "name": "Launch MCP Server (built)",
      "program": "${workspaceFolder}/dist/src/server.js",
      "preLaunchTask": "Build TypeScript",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
